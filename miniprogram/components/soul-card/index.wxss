/* components/soul-card/index.wxss — 灵魂匹配卡样式 */

.soul-card {
  background-color: var(--color-paper);
  border-radius: 20rpx;
  box-shadow: 0 2rpx 8rpx rgba(58, 42, 26, 0.08);
  padding: 24rpx;
  border: 2rpx solid var(--color-gold);
  transition: transform 0.15s ease;
}

.soul-card:active {
  transform: scale(0.98);
}

.card-header {
  display: flex;
  align-items: center;
  margin-bottom: 16rpx;
}

.avatar {
  width: 96rpx;
  height: 96rpx;
  border-radius: 48rpx;
  background-color: var(--color-primary);
  display: flex;
  align-items: center;
  justify-content: center;
  margin-right: 16rpx;
  flex-shrink: 0;
}

.avatar-char {
  font-size: 40rpx;
  color: #ffffff;
}

.user-info {
  flex: 1;
}

.user-name {
  font-size: 28rpx;
  color: var(--color-ink);
  font-weight: bold;
  display: block;
  margin-bottom: 6rpx;
}

.score-wrap {
  display: flex;
  align-items: center;
  gap: 6rpx;
}

.score-label {
  font-size: 20rpx;
  color: var(--color-ink-secondary);
}

.score-value {
  font-size: 22rpx;
  font-weight: bold;
  background-color: var(--color-primary);
  color: #ffffff;
  border-radius: 100rpx;
  padding: 6rpx 20rpx;
  min-width: 80rpx;
  text-align: center;
}

.intro-area {
  margin-bottom: 16rpx;
}

.intro-text {
  font-size: 24rpx;
  color: var(--color-ink-secondary);
  line-height: 1.7;
}

.common-tags {
  display: flex;
  flex-wrap: wrap;
  gap: 8rpx;
  align-items: center;
}

.common-tag {
  background-color: var(--color-primary);
  color: #ffffff;
  font-size: 20rpx;
  padding: 4rpx 12rpx;
  border-radius: 16rpx;
}

.more-tags {
  font-size: 20rpx;
  color: var(--color-ink-secondary);
}
