/* components/seal-stamp/index.wxss — 蜡封印章样式 */

.stamp-wrap {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 6rpx;
}

.stamp-circle {
  width: 88rpx;
  height: 88rpx;
  border-radius: 44rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  border: 4rpx solid var(--color-seal);
  transition: all 0.2s ease;
}

.stamp-sealed .stamp-circle {
  background-color: transparent;
  opacity: 0.4;
}

.stamp-open .stamp-circle {
  background-color: var(--color-seal);
  opacity: 1;
  box-shadow: 0 4rpx 20rpx rgba(181, 52, 30, 0.35);
}

.stamp-char {
  font-size: 36rpx;
  font-weight: bold;
  color: var(--color-seal);
}

.stamp-open .stamp-char {
  color: #ffffff;
}

.stamp-label {
  font-size: 18rpx;
  color: var(--color-ink-secondary);
}
